spring:
  datasource:
    url: jdbc:postgresql://localhost:5434/bddfithub
    username: postgres
    password: abc123
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.spatial.dialect.postgis.PostgisDialect
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        ddl-auto: none
        dialect: org.hibernate.dialect.PostgreSQLDialect
    open-in-view: true

# Configuración para la API de Exercisedb
exercisedb:
  api-key: a7a76cf85cmsh655ed18dbcb2a2dp1c56bfjsn8abedd3c1d3a

